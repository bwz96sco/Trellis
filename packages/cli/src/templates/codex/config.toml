# Trellis Research uses AGENTS.md as the project instruction file.
project_doc_fallback_filenames = ["AGENTS.md"]

# Project hooks require `[features].hooks = true` in the user's Codex config
# and, where prompted by Codex, one-time approval through `/hooks`.

# CS4-1/2 runtime notes

- `parseCanonicalMethodologyClosureArtifact` enforces closed schema + XOR + evidence.
- `record-result` binds closure ArtifactRef before mutation; Result.status is terminal only.
- `loadArtifactContractsFromProcedure` retains all pack contracts; no invented defaults.
- `deriveAcceptedV13PackIdentity` hashes allowlisted members; no caller-stamped digest.
- report-v2 materialization requires procedure id/digest + methodology digest + explicit closure + validation ok + batch commit.

# T6 MAL-1 Attempt-4 M0 correction implementation plan

## Completed correction implementation

1. Exact-three correction commit `fdb2c97a10f445e8b1cdfe8c3a05cea9497010c2` is committed on parent `4b1bb3cd34e574888025187247c2288f3be5195d` with tree `16b568ce95b2147fbe2bbddc5dc4fc224c59d731`.
2. Its inventory is exactly:
   - `.trellis/spec/cli/unit-test/conventions.md`
   - `packages/cli/scripts/research-v131-installed-package-audit-i3.mjs`
   - `packages/cli/test/commands/research-v131-integration-i3.test.ts`
3. Normal hooks passed. No correction write or commit authority remains pending.

## Governance commit

1. Require exact correction commit `fdb2c97a` as `HEAD` and as this governance commit's direct parent; authenticate its parent, tree, and exact-three inventory.
2. Confirm the worktree contains only this untracked exact-six governance task and no restored M0-A4 path.
3. Encode the committed correction identity, exact governance/M0/prospective-M1 inventories, protected topology, and denied authorities.
4. Parse JSON and every JSONL line.
5. Validate the task, supported formatting, retained I3, diff checks, exact-six inventory, protected topology, and GitNexus staged/compare scopes.
6. Keep task status `in_progress` for the committed record.
7. Commit exact-six through normal hooks with complete output and process/memory telemetry.
8. Authenticate parent `fdb2c97a`, resulting tree, exact inventory, modes, blobs, protected topology, and clean status.
9. Stop pending a fresh M0-A4 runtime instruction.

## Future restored M0-A4

After separate fresh authorization:

1. Require the exact committed governance boundary as `HEAD` and correction `fdb2c97a` as its authenticated parent.
2. Restore the preserved M0 exact-three candidate.
3. Rebind it to the committed correction and governance identities, with the M0 commit directly descending from governance.
4. Replace boolean-only M1 confirmation with committed M0 and M1 authority authentication.
5. Require fixed M1 exact-six root `.trellis/tasks/08-21-authorize-t6-mal1-attempt-4-run/`.
6. Run GitNexus impact before Python symbol edits.
7. Run reviewer self-check, retained I3 verification, task/JSON validation, exact-three inventory checks, focused tests, formatting, and GitNexus detection.
8. Commit exact-three through normal hooks; authenticate and stop before M1.

## Stop routes

Stop on correction or governance identity drift, protected drift, generic scope allowance, serialized evidence mutation, missing committed M1 authentication, extra path, failed hook, reviewer launch, Attempt-4 output, network/provider action, or collapsed authority. Preserve failure logs and move forward only.

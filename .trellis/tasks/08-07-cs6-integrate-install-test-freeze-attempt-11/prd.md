# CS6-6 — Integrate, install-test, and freeze attempt 11

## Goal

Integrate the accepted dormant CS6 candidate, prove real npm/pnpm archive and external-install behavior, verify historical Procedure bytes without relying on `.git` inside the subject extraction, and freeze exact attempt-11 subject S11 with a resolved one-file record.

## Dependencies

- CS6-0 governance is frozen.
- CS6-1 is committed as `leaves-sound`.
- CS6-2 through CS6-5 are independently accepted and committed.
- Exact accepted contract, `2.0.7` package, runtime, CLI, and harness identities are available.
- Separate activation and integration/freezer assignment are required.

## Ownership

Owned integration/test paths:

- `packages/cli/test/commands/research-cs6-integration.test.ts` (new)
- `packages/cli/scripts/research-cs6-installed-package-audit.mjs` (new)

Owned evidence/freeze paths:

- `.trellis/tasks/08-07-cs6-integrate-install-test-freeze-attempt-11/research/**`

Normative future evidence includes archive-safe historical blob locks, real tarball/install evidence, an I11 integration record, and exactly one S11 freeze record.

## Requirements

- Build real core and CLI tarballs and record exact tarball digests/inventories.
- Install through npm and pnpm into external temporary repositories outside the source tree.
- Exercise installed accepted-bundle authentication, exact 17-package `2.0.7` inventory, record-result, replay, report-v2, update/reinstall behavior, and dormant/live authority flags.
- Prove Procedure `2.0.4`–`2.0.6` preservation using committed Git blob inventories or subject-carried locks; tests must not call `git show HEAD:<path>` inside a git-archive extraction.
- Run the full predetermined integration corpus with deterministic isolation and no hidden source-tree fallback.
- Produce I11 only after all integration/install checks pass.
- Produce S11 as a separate exact freeze boundary after I11. The S11 record contains resolved subject commit/tree/input identities and must not contain `S-commit-hash-filled-at-freeze` or another self-hash placeholder.
- Keep live Procedure `1.0.0` and all `2.0.7` authority flags false.

## Exclusions

No technical repair to CS6-2 through CS6-5 owned paths, no accepted/historical/package byte rewrites, no registry/live-selection edits, no CS5 record edits, no assurance outputs, no operator record, no specs, and no `.trellis/research/**`.

## Activation gate

Separate user approval after exact accepted CS6-1 through CS6-5 commits exist.

## Stop gates

- Any upstream test fails or input identity drifts.
- External install falls back to source-tree bytes.
- Historical proof requires `.git` in the extracted subject.
- Integration discovers a defect owned by a prior child; return it rather than repair here.
- S11 cannot be frozen without a placeholder or mixed technical changes.

## Commit boundaries

- Future **I11** may include only owned integration script/test and integration/install evidence.
- Future **S11** is a separate one-file freeze commit containing only the resolved S11 freeze record and task metadata if explicitly authorized.
- Neither boundary authorizes assurance, operator decision, activation, archive, release, publication, or push.

## Authority flags

All human-review/equivalence, repair, complete-system acceptance, operator, activation, archive, release, publication, and push flags remain false.

## Acceptance criteria

- [ ] Real core/CLI tarballs are inventoried, digested, and externally installed with npm and pnpm.
- [ ] Installed behavior does not read source-tree-only assets.
- [ ] Historical `2.0.4`–`2.0.6` proof works in a git-archive extraction.
- [ ] All accepted contract/package/runtime/harness/integration checks pass.
- [ ] I11 remains dormant with live `1.0.0`.
- [ ] S11 is exact, separate, resolved, and placeholder-free.

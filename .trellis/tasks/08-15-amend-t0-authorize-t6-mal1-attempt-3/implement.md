# T0A — T6 MAL-1 Attempt-3 implementation plan

## Ordered execution

1. Validate and commit this exact six-file standalone T0A overlay.
2. Run GitNexus upstream impact on every existing reviewer-program symbol before editing it; stop and warn on HIGH or CRITICAL risk.
3. Modify exactly the existing three T6 M0 paths:
   - authenticate corrected T4, I2, S2, Attempts 1–2, and Attempt-3 T0A in forward lineage;
   - route output to `research/attempt-3`;
   - add bounded privacy-safe failed-command diagnostics and stable identifiers;
   - add explicit blocked prerequisite reasons;
   - preserve separate T4 code-presence and production-prevention observations with OR acceptance;
   - update exact task/assignment authority and focused self-checks.
4. Run compile, focused self-checks, task validation, diff check, exact staged-path verification, and staged GitNexus detection.
5. Commit the exact three-path M0 correction and independently review that commit.
6. Run authoritative Attempt-3 while the protected main worktree remains exact S1, using detached execution rather than a short outer command timeout.
7. Independently review the exact nine outputs. Commit honest evidence only after exact-inventory, containment, privacy, diagnostic-bound, and internal-consistency checks.
8. Do not start T7, push, publish, release, activate, or execute a provider.

## Verification

- Use `uv run python` for Python execution.
- Confirm Attempts 1 and 2 bytes are unchanged.
- Confirm the controlled path retains exactly the eleven authenticated executable names.
- Confirm no authoritative Attempt-3 directory exists before the run.
- Exercise diagnostic bounding, redaction, timeout/launch failure, and blocked prerequisite self-check fixtures.
- Confirm exact 26-command observation and exact nine-file publication afterward.
- Run `git diff --check` and staged GitNexus change detection before each commit.

## Stop routes

Stop on any immutable-input, ancestry, path-inventory, tool-identity, protected-root, diagnostic-privacy, provider, network, or destination mismatch. Route T1–T5 technical defects to their owning stage. Correct T6 harness defects only through another forward-only T6 governance boundary.

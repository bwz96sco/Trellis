# Implementation plan

## Step 0 — Baseline, payload snapshot, and impact

- Confirm Child A is archived and Child C remains planning-only.
- Snapshot current Claude/Codex collector keys and fresh init/update outputs.
- Record protected `.trellis/research/**`, workflow ownership, hash manifest, structured user-content, and historical generic fixtures.
- Run GitNexus upstream impact for every existing symbol before editing it, including:
  - `init`;
  - `handleReinit`;
  - `createRootFiles`;
  - `createWorkflowStructure`;
  - `configureClaude`;
  - `configureCodex`;
  - `configurePlatform`;
  - `collectPlatformTemplates`;
  - `collectClaudeTemplates`;
  - `collectCodexTemplates`;
  - any new Research resolver integration point;
  - `collectTemplateFiles`;
  - `buildAgentsMdTemplate`;
  - `update`;
  - any changed hook registry function.
- Warn before HIGH/CRITICAL edits. Split rather than cross Child C/compatibility boundaries.

Verify baseline focused init/update/configurator/hook/C07/C09/cleanup suites pass.

## Step 1 — Define canonical active Research payload

- Add one typed Research-only selector/allowlist shared by Claude/Codex configure and collect paths.
- Include exactly nine canonical stage skills, one bounded worker per host, exact hooks/mixed paths, and optional Claude statusline.
- Keep historical cleanup inventory independent.
- Keep generic resolvers/source callable only for inactive compatibility tests until Child C.

Verify:

- exact sorted path sets;
- retained worker/stage completeness;
- no active retired opaque path;
- configure/collect two-way byte parity.

Rollback point: selector plus focused tests only.

## Step 2 — Narrow Claude and Codex configurators/collectors

- Route Claude/Codex active generation through the shared Research selector.
- Remove active generic commands, generic agents, common workflow skills, and non-Research bundled skills.
- Claude emits three Research hooks and optional Research statusline.
- Codex emits one registered Research sequence hook and no unregistered `session-start.py`.
- Preserve user config merge behavior and neutral `.agents/skills` rendering.

Verify exact output for Claude-only, Codex-only, dual-host, statusline on/off, and init/update parity.

## Step 3 — Rewrite hooks before removing scripts

- Reduce `inject-subagent-context.py` to explicit C09 worker preflight only.
- Rewrite `session-start.py` to direct compact Research orientation without generated scripts or generic state.
- Rewrite `inject-workflow-state.py` to strict Research ledger sequence watermark only for registered hosts.
- Remove generic Codex session hook from active output.
- Rewrite Claude statusline to direct bounded Research state.
- Align shared hook registry and structured host registrations exactly.

Verify:

- no `.trellis/scripts`, Task, spec, workspace, developer, Channel, or generic agent references in generated hooks;
- unchanged/changed/malformed sequence matrix;
- C07/C09 exact envelope, denial, optional skill, parity, and zero-write tests;
- generated hook set equals registered hook set.

Rollback point: active collector still may generate scripts until hook tests pass.

## Step 4 — Make base Research layout minimal

- Update `createWorkflowStructure({ layout: "research" })` to skip scripts before any copy.
- Rewrite Research workflow, config, gitignore, and root `AGENTS.md` managed template.
- Keep legacy source branches only as inactive compatibility input.

Verify exact minimal base layout and negative generic-token scan of generated bytes.

## Step 5 — Narrow init/re-init

- Remove active project-type, monorepo, registry/spec, developer, joiner Task, and bootstrap Task paths.
- Reject generic-only options before writes until Child C unregisters parser flags.
- Preserve selected-host defaults, local bundled Research workflow, force/skip behavior, add-host workflow ownership restoration, actual-write tracking, and lazy Research state.

Verify:

- fresh Claude/Codex/dual exact layouts;
- no scripts/agents/tasks/workspace/spec/developer/research directory;
- host addition preserves workflow bytes/hash/selection;
- force re-init idempotency;
- generic-only options are zero-write failures.

## Step 6 — Narrow update desired state

- Remove scripts, legacy agents, registry config/spec refresh, and generic assets from `collectTemplateFiles()`.
- Consume Research-only host collectors and mixed templates.
- Remove migration Task creation and `.developer` dependency.
- Preserve planning, backup, workflow classifier/apply, manifest pruning, migrations, safe-delete, version, and hash ordering.

Verify:

- update does not recreate retired files;
- pristine v0.6.7 safe-delete fires after active ownership disappears;
- modified/unapproved historical bytes survive;
- no migration Task or registry/spec write;
- fresh init and post-update retained bytes are identical;
- repeated update is zero-write/idempotent.

## Step 7 — Structured compatibility and protected state

Exercise:

- Claude settings unrelated fields/hooks survive;
- Codex hooks/config unrelated content survives;
- malformed mixed files remain byte-preserved;
- `AGENTS.md` user content outside markers survives;
- statusline opt-in remains deterministic;
- `.trellis/research/**` before/after snapshots are byte-identical;
- frozen cleanup sets/cardinalities remain unchanged;
- unknown descendants remain unowned;
- generic source remains present but inactive.

## Step 8 — Code-spec update

Update executable contracts with all seven sections where behavior changes:

- minimal Research init/re-init layout and unsupported generic options;
- canonical Research payload selector and configure/collect parity;
- Research-only hook matrix and no-script runtime;
- Research-only update desired state and no migration Task;
- workflow/config/gitignore/AGENTS mixed ownership;
- active-versus-historical cleanup separation.

Do not update Child C command-removal/package-deletion contracts yet.

## Step 9 — Independent implementation/check loop

1. Validate task context.
2. Activate Child B.
3. Dispatch `trellis-implement` with this bounded scope.
4. Run focused tests.
5. Dispatch independent `trellis-check`.
6. Fix only confirmed Child B defects.
7. Repeat until no blocker.

No agent commit.

## Step 10 — Verification

Focused suites should include at minimum:

```bash
pnpm --filter @mindfoldhq/trellis exec vitest run \
  test/configurators/platforms.test.ts \
  test/configurators/index.test.ts \
  test/commands/init-research-only.integration.test.ts \
  test/commands/init.integration.test.ts \
  test/commands/init-joiner.integration.test.ts \
  test/commands/update-internals.test.ts \
  test/commands/update.integration.test.ts \
  test/commands/update-workflow-classifier.test.ts \
  test/commands/update-workflow-migration.integration.test.ts \
  test/templates/shared-hooks.test.ts \
  test/templates/research-hooks.test.ts \
  test/templates/claude.test.ts \
  test/templates/codex.test.ts \
  test/templates/hook-timeouts.test.ts \
  test/templates/trellis.test.ts \
  test/legacy/current-host-generic-cleanup.test.ts \
  test/utils/manifest-prune.test.ts \
  test/commands/uninstall.integration.test.ts \
  test/commands/init-uninstall-overdelete.integration.test.ts \
  test/regression.test.ts
```

Package/workspace:

```bash
pnpm --filter @mindfoldhq/trellis test
pnpm --filter @mindfoldhq/trellis-core test
pnpm --filter @mindfoldhq/trellis lint
pnpm --filter @mindfoldhq/trellis lint:py
pnpm --filter @mindfoldhq/trellis typecheck
pnpm typecheck
pnpm --filter @mindfoldhq/trellis build
git diff --check
```

Also verify:

- source/build bytes for retained hooks/workers/stage skills;
- no generated active output contains forbidden generic paths/tokens;
- canonical `.trellis/research/**` unchanged;
- `docs-site` and `marketplace` untouched;
- no Child C command/source/package deletion.

## Step 11 — Close

- Check every acceptance criterion from evidence.
- Run GitNexus change detection and separate inherited C01–C10 breadth from Child B symbol risk.
- Archive Child B with `--no-commit`.
- Only then activate Child C.

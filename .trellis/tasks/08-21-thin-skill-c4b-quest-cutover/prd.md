# Add Research Quest Cutover

## Goal

Deferred child for exact Quest import/export and enforced Trellis/source single-writer authority transfer after canonical H1/H2 gates are complete.

## Requirements

- Preserve the parent task's exact source-field mapping, export reconstruction, blocking-conflict, authority-projection, and source-admin pre-write refusal contracts.
- Do not start until minimal C4 scientific gates are implemented, verified, committed, and archived.
- Re-plan this child against the then-current source `research-quest-admin` bytes before implementation approval.

## Acceptance Criteria

- [ ] Exact Quest import/export mapping is converged against current authenticated source bytes.
- [ ] Trellis/source dual-write is structurally impossible after cutover.
- [ ] Source read-only validation remains available.
- [ ] Rollback requires explicit verified export and authority transfer.

## Notes

This task was split from the original combined C4 scope. It is intentionally deferred; no Quest cutover work belongs in minimal C4.

# T5 — Integrate, externally install, and freeze v1.3.1 subject design

## Boundary

Integrate accepted technical outputs, prove real tarball lifecycle behavior in external repositories, then freeze one exact placeholder-free subject under a separate one-file boundary. The stage consumes immutable predecessor Git objects and writes only its exact inventory. Mutable worktree candidate bytes are never semantic authority.

## Data and authority flow

```text
exact committed predecessor inputs
  -> strict authentication
  -> integration-freeze-owner-unassigned
  -> exact stage-owned outputs
  -> deterministic verification
  -> separately authorized immutable commit boundary
```

## Ownership model

I1 owns one integration test, one installed-package audit script, task status, and exact integration evidence. S1 owns exactly one subject-freeze JSON. T5 cannot repair T1-T4 paths.

Cross-stage calls use reviewed exported interfaces. A required path outside the closed inventory returns to T0 for an ownership amendment; it is not edited opportunistically.

## Compatibility

Historical contract and Procedure identities retain recorded interpretation. New v1.3.1 technical behavior is version-explicit. Live Procedure remains `1.0.0`; Procedure `2.0.7` is dormant. Worker Proposal-only and root Decision boundaries remain unchanged.

## Role separation

The accountable actor for this stage must match `integration-freeze-owner-unassigned` and satisfy the T0 identity-separation matrix. T6 cannot share an identity or scratch space with T0-T5. T7 must be a separately instructed accountable operator.

## Failure and rollback

Before commit, remove only uncommitted files from this stage's exact allowlist. After commit, corrections are forward-only. Upstream defects return to the owning stage; assurance and decision stages never repair technical inputs.

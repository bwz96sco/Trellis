# T0A — T5 successor refreeze implementation plan

## Ordered execution

1. Validate and commit this exact six-file governance overlay.
2. Create only the exact eight I2 paths listed in `task.json`.
3. Copy the accepted T5 audit/test mechanism to successor paths; update only successor task roots, names, messages, and corrected T4 commit/tree bindings.
4. Install dependencies offline if absent, build Core before CLI consumers, and run syntax, focused integration, typecheck, packed audits, and installed-package `--write` then `--verify`.
5. Record honest command results and implementation/evidence digests in the I2 execution ledger.
6. Verify exact I2 inventory, protected state, diff checks, and staged GitNexus scope; commit I2 separately.
7. Generate S2 from committed I2 Git objects with no placeholders, verify exact one-path scope and GitNexus, then commit S2 separately.
8. Remove only verification directories known absent before this stage. Do not begin T6 Attempt-3 in either T5 commit.

## Stop routes

Stop on old T5/T6 mutation, production-source change, semantic/package identity drift, non-offline package resolution, protected-path mismatch, unknown output, predecessor mismatch, or operational authority expansion.

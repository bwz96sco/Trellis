# T0A — Authorize T6 MAL-1 Attempt-2

## Goal

Preserve committed T6 Attempt-1 as immutable harness-failure evidence, authorize the minimum reviewer-program correction, and authorize one forward-only Attempt-2 against the unchanged exact S1 subject.

## Normative authority

`task.json` is the sole normative authorization record. The other five standard files explain and validate that boundary.

## Confirmed failure

Attempt-1 commit `cd85634` completed the exact 26-row command inventory and exact nine-file publication, but the controlled `PATH` omitted `sh`, `sed`, `dirname`, and `uname`. This caused `spawn sh ENOENT` for pnpm lifecycle scripts and made Vitest wrappers fail before loading tests. The failure does not establish a T1–T5 technical defect.

## Requirements

1. Keep every Attempt-1 byte and commit immutable.
2. Correct only the existing three T6 M0 files.
3. Add only `sh`, `sed`, `dirname`, and `uname` to the authenticated controlled executable inventory.
4. Update exact ancestry, changed-path authentication, self-check expectations, assignment/task authority text, and the destination from `attempt-1` to `attempt-2`.
5. Preserve the exact S1 subject, reviewer identity separation, no-shared-T0–T5-scratch declaration, network denial, provider tripwires, protected-root checks, atomic exact-nine publication, and every operational denial.
6. Permit the already assigned T6 reviewer session to resume. Freshness means the stage actor is distinct from T0–T5 and does not share their scratch; it does not require discarding the assigned T6 session between M0 and M1.
7. Run Attempt-2 before any fast-forward of `evidence/v13-baseline`, because the protected root must remain exact S1 during assurance.
8. If Attempt-2 fails, preserve its evidence and route the defect without repair inside assurance.

## Exact inventories

- T0A owns exactly its six standard task files.
- Attempt-2 M0 correction owns exactly the existing three T6 M0 paths.
- Attempt-2 M1 owns exactly nine files under `.trellis/tasks/08-12-assure-v1-3-1-complete-system-mal1/research/attempt-2/` with the same filenames as Attempt-1.

## Acceptance criteria

- [ ] T0A commits exactly six files.
- [ ] Attempt-1 commit `cd85634` remains unchanged.
- [ ] The M0 correction is a direct forward descendant and changes exactly three paths.
- [ ] The controlled executable inventory adds exactly four confirmed POSIX tools and no broader host `PATH`.
- [ ] Focused self-checks and task validation pass.
- [ ] Independent review accepts the exact M0 correction.
- [ ] Attempt-2 emits exactly nine files with honest pass/fail evidence.
- [ ] No provider, network, activation, acceptance, release, publication, push, live-selection, worker-authority, or T7 action occurs.

## Out of scope

Any T1–T5 source repair, frozen-subject change, Attempt-1 rewrite, broad host-PATH restoration, new provider tracing framework, T7 decision, activation, publication, release, or push.

# T0A — T6 MAL-1 Attempt-2 implementation plan

## Ordered execution

1. Validate and commit this exact six-file standalone T0A overlay.
2. Run GitNexus upstream impact on every existing reviewer-program symbol before editing it.
3. Modify exactly the existing three T6 M0 paths:
   - authenticate `cd85634` and the T0A commit in the forward lineage;
   - preserve exact prior M0 and Attempt-1 changed-path inventories;
   - route outputs to `research/attempt-2`;
   - add only `sh`, `sed`, `dirname`, and `uname` to the controlled executable inventory;
   - update exact task/assignment authority and focused self-checks.
4. Run compile, focused self-checks, task validation, diff check, exact staged-path verification, and staged GitNexus detection.
5. Commit the exact three-path M0 correction and independently review that commit.
6. Run authoritative Attempt-2 while the protected main worktree remains exact S1.
7. Independently review the exact nine outputs. Commit honest evidence only after exact-inventory, containment, privacy, and internal-consistency checks.
8. Fast-forward main only after the accepted Attempt-2 commit. Do not start T7.

## Verification

- Use `uv run python` for Python execution.
- Require the reviewer self-check to authenticate the full forward lineage and exact path sets.
- Confirm Attempt-1 bytes are unchanged.
- Confirm the controlled path contains the eleven authenticated executable names and does not restore the broad host path.
- Confirm no authoritative Attempt-2 directory exists before the run.
- Confirm exact 26-command observation and exact nine-file publication afterward.
- Run `git diff --check` and staged GitNexus change detection before each commit.

## Stop routes

Stop on any immutable-input, ancestry, path-inventory, tool-identity, protected-root, privacy, provider, network, or destination mismatch. Route T1–T5 technical defects to their owning stage. Correct T6 harness defects only through another forward-only T6 governance boundary.

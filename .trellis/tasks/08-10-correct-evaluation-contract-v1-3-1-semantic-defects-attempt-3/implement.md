# Attempt-3 execution plan

1. G133: freeze exact governance, topology, inventories, target closure, authority, and validation; commit exact 36 paths.
2. A133-0: assign a fresh author in exactly two paths.
3. A133-1: deterministically generate exactly 15 outputs; verify ten unchanged/five changed and two clean identical runs.
4. B133-0: assign a fresh non-fork reviewer in exactly two paths.
5. B133-1: independently review exact Git archives, emit exactly 11 outputs, and commit honest pass/fail.
6. STOP. O133 requires a later genuine operator instruction.

## Validation
Use `uv run python`; strict UTF-8 and duplicate-key JSON; exact path inventories; canonical JSON; task validation; protected fingerprints; empty staged set; `git diff --check`; GitNexus detect-changes before commit.
